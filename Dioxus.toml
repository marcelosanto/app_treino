[application]

[web.app]

# HTML title tag content
title = "app_treino"

# include `assets` in web platform
[web.resource]

# Additional CSS style files
style = []

# Additional JavaScript files
script = []

[web.resource.dev]

# Javascript code file
# serve: [dev-server] only
script = []

[build]
# Embute os arquivos de public/ no binário final
embed_assets = true
out_dir = "dist"
